<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Create Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            animation: {
              fadeIn: "fadeIn 1s ease-out both",
            },
            keyframes: {
              fadeIn: {
                "0%": { opacity: 0, transform: "translateY(20px)" },
                "100%": { opacity: 1, transform: "translateY(0)" },
              },
            },
          },
        },
      };
    </script>
  </head>
  <body class="bg-gradient-to-br from-indigo-950 via-zinc-900 to-purple-900 min-h-screen flex items-center justify-center text-white font-sans">
    <div class="w-full max-w-lg p-10 bg-white/10 border border-white/20 rounded-3xl shadow-2xl backdrop-blur-xl animate-fadeIn">
      <div class="text-center mb-8">
        <h1 class="text-4xl font-bold text-white">Create Your Account ✨</h1>
        <p class="text-white/70 mt-2 text-sm">Join us and start posting your thoughts</p>
      </div>

      <form action="/register" method="POST" class="space-y-5">
        <input
          type="text"
          name="username"
          placeholder="Create Username"
          required
          class="w-full px-4 py-3 rounded-xl bg-white/10 border border-white/30 placeholder-white/70 text-white focus:outline-none focus:ring-2 focus:ring-pink-400 transition"
        />
        <input
          type="text"
          name="name"
          placeholder="Your Name"
          required
          class="w-full px-4 py-3 rounded-xl bg-white/10 border border-white/30 placeholder-white/70 text-white focus:outline-none focus:ring-2 focus:ring-pink-400 transition"
        />
        <input
          type="text"
          name="age"
          placeholder="DOB"
          onfocus="this.type='date'"
          onblur="this.type='text'"
          required
          class="w-full px-4 py-3 rounded-xl bg-white/10 border border-white/30 placeholder-white/70 text-white focus:outline-none focus:ring-2 focus:ring-pink-400 transition"
        />
        
        <input
          type="password"
          name="password"
          placeholder="Create Password"
          required
          class="w-full px-4 py-3 rounded-xl bg-white/10 border border-white/30 placeholder-white/70 text-white focus:outline-none focus:ring-2 focus:ring-pink-400 transition"
        />

        <button
          type="submit"
          class="w-full bg-pink-600 hover:bg-pink-700 text-white font-semibold py-3 rounded-xl shadow-lg hover:shadow-pink-500/30 transition"
        >
          Create Account
        </button>
      </form>

      <p class="mt-6 text-center text-white/70 text-sm">
        Already have an account?
        <a href="/login" class="text-pink-400 hover:underline">Log in</a>
      </p>
    </div>
  </body>
</html>
